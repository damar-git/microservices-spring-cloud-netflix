server:
  port: 8888

spring:
  application:
    name: config-server-app
  profiles:
    active: native
  cloud:
    config:
      discovery:
        enabled: true
      server:
        native:
          searchLocations: file:///${user.home}/GitRepositories/microservices_/config-data